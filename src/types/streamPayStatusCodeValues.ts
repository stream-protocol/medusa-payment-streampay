// Define an enum for StreamPayStatusCodeValues
enum StreamPayStatusCodeValues {
  Success = 'success',
  Failure = 'failure',
  Pending = 'pending',
  // Add other status codes as needed
}

// Export the StreamPayStatusCodeValues enum
export default StreamPayStatusCodeValues;
